<h1>Categories</h1>
<%=link_to 'New Register', url_for(action: :new), class: 'btn btn-primary' %>

<%= will_paginate @categories %>
<table class="table" id="categories">
  <thead class="thead-light">
    <tr>
      <th>#</th>
      <th>Title</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <%= render @categories %>
  </tbody>
</table>
<%= will_paginate @categories %>

