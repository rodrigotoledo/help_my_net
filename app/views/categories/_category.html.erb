<tr id="<%= dom_id category %>">
  <td>
    <%= category.id %>
  </td>
  <td>
    <%= link_to category.title, edit_category_path(category) %>
  </td>
  <td>
    <%= button_to "Remove this", category_path(category), method: :delete, class: 'btn btn-danger' %>
  </td>
</tr>
